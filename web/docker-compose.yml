version: '3.8'
services:
  car-manager:
    image: car-manager
    #image: ghcr.io/felipeab10/car-manager:latest
    container_name: car-manager
    build:
      context: ./
      target: production
      dockerfile: Dockerfile
    volumes:
        - .:/app
        - /app/node_modules
        - /app/.next
    #ports:
     # - "3000:3000"